import { AccordionControlDirective } from './accordion-control.directive';

describe('AccordionControlDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionControlDirective();
    expect(directive).toBeTruthy();
  });
});
